import { TeaShopValidators } from './tea-shop-validators';

describe('TeaShopValidators', () => {
  it('should create an instance', () => {
    expect(new TeaShopValidators()).toBeTruthy();
  });
});
